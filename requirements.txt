# Runtime dependencies, pinned to the versions the app is tested against so
# deploys are reproducible. Bump deliberately, not implicitly.
fastapi==0.141.1
uvicorn==0.52.4
python-dotenv==1.2.3
gspread==6.2.1
google-auth==2.57.0
langgraph==1.2.11
dspy==3.3.1
httpx==0.28.1
pydantic==2.13.4
# Timezone database for zoneinfo (Windows and slim Linux images have no system tzdata).
tzdata>=2024.1
